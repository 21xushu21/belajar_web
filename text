Route::get('/pengajuan/renovasi', [SubmissionInputController::class, 'form_renovasi'])->name('user.form_renovasi');


<a href="{{ route('atasan.submission.renovation', $data->id) }}">


class Area extends Model
{
    use HasFactory;
    protected $table = "bsi_area";
    public $timestamps = false;
    protected $guarded = [];


     public function users(){
        return $this->hasMany(User::class, 'id', 'area');
    }
}
