Route::get('/pengajuan/renovasi', [SubmissionInputController::class, 'form_renovasi'])->name('user.form_renovasi');


<a href="{{ route('atasan.submission.renovation', $data->id) }}">


class Rent extends Model
{
    use HasFactory;
    protected $table = "bsi_harga_sewa";
    public $timestamps = false;
    protected $guarded = [];
}

$total_pengajuan += HouseProcurement::whereUserId(auth()->user()->id)->count();
        $total_pengajuan += ServiceProcurement::whereUserId(auth()->user()->id)->count();
        $total_pengajuan += RenovationProcurement::whereUserId(auth()->user()->id)->count();

        return view('user.index', compact('total_pengajuan'));
